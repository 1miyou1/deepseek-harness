/** Pure host half for the browser-discovered Module Scheduler UI plugin. */

/** Host plugin body; scheduler behavior lives in the browser export and Host RPC domain. */
export function apply(): void {}
